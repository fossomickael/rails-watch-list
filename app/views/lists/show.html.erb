<h1><%= @list.name %></h1>

<h1>Add a bookmark</h1>
<%= simple_form_for([@list, @bookmark]) do |f| %>
     <%= f.input :comment %>
     <%= f.submit "ajouter", class: 'btn btn-primary w-100' %>
<% end %>