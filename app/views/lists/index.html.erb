

<div class="banner banner-index">
  <div class="container text-center ">
    <h1>Save any kind of movies</strong></h1>
    <p>Classic</p>
    
  </div>
</div>

<div class="container">
   <h2>My Lists</h2>
  <div class="row">
    <% @lists.each do |list| %>
    <div class="col-4 py-2">
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://source.unsplash.com/random?sig=<%= rand(1..1000) %>)">
            <%= link_to list.name, list_path(list), class: "white" %>
        </div>
    </div>
   <% end %>
  </div>
</div>